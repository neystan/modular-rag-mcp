请基于下面的 chunk 内容生成结构化元数据，返回 JSON 对象，不要输出额外说明。

要求：
1. title: 生成简洁标题，控制在 24 个字以内
2. summary: 生成 1-2 句摘要，保留关键事实
3. tags: 提取 3-5 个标签，标签使用短语，不要重复
4. 输出必须是合法 JSON，格式如下：
{{
  "title": "...",
  "summary": "...",
  "tags": ["...", "..."]
}}

文本：
{text}
